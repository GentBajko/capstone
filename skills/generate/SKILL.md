---
name: generate
description: Use when asked to build or rebuild a codebase's architecture reference docs from scratch (DESIGN.md plus docs/capstone/ topic files), or to regenerate one topic chapter - maps the architecture, data models, conventions, and module boundaries so AI sessions read docs instead of re-exploring the repo. Incremental refresh is the sync skill's job.
---

# Capstone: generate

From this skill's base directory, read `../core/references/core.md`, then
execute
`../core/references/protocols/generate.md`
exactly.
